import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <div className="header">
            {/* Header content here */}
        </div>
    );
};

export default Header;
